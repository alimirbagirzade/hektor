# Achilles unattended training handoff — 2026-08-03

## Canlı durum

- Eğitim: `achilles_lora_v5_durable`, `bf16`, `discipline_safe_local`, hedef `2406`.
- Son doğrulanan ilerleme: `25/2406`.
- İlk dayanıklı kayıt: `models/adapters/achilles_lora_v5_durable/checkpoint-25`.
- Training Guardian: `running`; Windows görevi `AchillesTrainingWatchdog` (5 dakika).
- Web: `AchillesWeb` görevi `Running`.
- Ajan haritası: 28 düğüm / 30 bağlantı.

## Kurtarma sözleşmesi

1. PEFT her 25 adımda checkpoint yazar ve son 3 checkpoint'i korur.
2. Eğitim prosesi yoksa watchdog `storage/train_status.json` reçetesini yeniden başlatır.
3. Trainer en yeni `checkpoint-*` klasörünü bulup otomatik resume eder.
4. Mutex + 10 dakikalık taze-log heartbeat ikinci eğitim ağacını engeller.
5. Web restart yalnız web prosesini sonlandırır; eğitim prosesine dokunmaz.
6. `storage/STOP_ALL` bütün otomatik yeniden başlatmaların üstündedir.

## Chroma/web izolasyonu

Windows'taki `chromadb_rust_bindings.pyd` erişim ihlali web prosesini öldürüyordu.
`ACHILLES_WEB_ISOLATE_CHROMA=1` ile web ve ajan haritası native Chroma işinden ayrıldı.
RAG/Chroma arızası artık paneli veya LoRA eğitimini düşürmez.

## Kaynak kontrolü

- PR: https://github.com/alimirbagirzade/achilles/pull/136
- Training Guardian: `55b5cb7`
- Web/Chroma izolasyonu: `38836f1`
- Dayanıklı checkpoint/watchdog: `7ef3ec7`
- Web restart eğitim-koruması: `a290b3d`

## Sonraki operatör için

- Eğitimi yeniden başlatma: normalde gerekmez; watchdog yapar.
- Durum: `.\scripts\start-train.ps1 -Status`
- Web/harita: `http://127.0.0.1:8765`
- Acil durdurma: `uv run achilles stop-all`
- `checkpoint-25` mevcut olduğundan bundan sonraki çökmede eğitim sıfırdan başlamaz.
